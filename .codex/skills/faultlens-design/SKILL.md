---
name: faultlens-design
description: Turn approved FaultLens .NET SDK decisions into an implementation-ready public contract, compatibility, privacy, runtime, and validation design.
---

# FaultLens Design — .NET SDK copy

Canonical source: `FaultLensHQ/faultlens-engineering/skills/design/SKILL.md` (governance v1.2.x).

Use after Product Decisions for material SDK/public-contract work. Define public API/config semantics, compatibility/migration/versioning, privacy/redaction defaults, host failure isolation, target/runtime boundaries from authoritative project config, backend/ingestion contracts, rollout and executable validation.

Return `READY FOR IMPLEMENTATION` only when execution is not expected to invent customer-visible or compatibility semantics; otherwise return `DESIGN BLOCKED`.
